import fetch from 'cross-fetch';

export class LogglyApi {
  token;
  tags;

  constructor(token, tags) {
    this.token = token;
    this.tags = tags;
  }

  // --------------------------------------------------------------------------------
  //  private
  // --------------------------------------------------------------------------------

}
